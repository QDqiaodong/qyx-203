server:
  port: 8133

spring:
  datasource:
    url: jdbc:h2:mem:airport_maternity_test;DB_CLOSE_DELAY=-1;MODE=MySQL
    username: sa
    password:
    driver-class-name: org.h2.Driver

  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false

logging:
  level:
    com.airport.maternity: INFO
