# 机场航站楼母婴室配套设备使用时段关联登记系统

## 项目简介

机场航站楼母婴室配套设备使用时段关联登记系统，核心特色为设备绑定固定使用时段区间。每件设备建档绑定日常固定投放使用时段；航站楼运营排班调整时修改设备对应时段；支持按时段区间筛选对应全部设备；可统计单台设备绑定的全部时段区间，生成时段分配明细。

## 技术栈

- **前端**: Vue3 + Vite + TypeScript + Element Plus
- **后端**: Spring Boot 3.3 + JDK 17
- **数据库**: MySQL 8.0
- **缓存**: Redis 7
- **容器化**: Docker + Docker Compose

## 访问地址

| 服务 | 地址 | 端口 |
|------|------|------|
| 前端页面 | http://localhost:8123 | 8123 |
| 后端API | http://localhost:8133 | 8133 |
| MySQL | 127.0.0.1:3349 | 3349 |
| Redis | 127.0.0.1:6422 | 6422 |

## 核心功能模块

### 1. 设备基础档案管理
- ✅ 设备编号、功能类型、航站楼分区的录入
- ✅ 设备查询、编辑、删除
- ✅ 按航站楼分区/设备类型筛选设备
- ✅ 设备停用联动：保存停用的同一事务内，名下尚未结束（进行中/未开始）的「生效中」占用全部置为「已失效」，已结束的历史时段不动；停用后不再按正常在用出现在时段列表与统计，也不能再挂「生效中」时段；每段失效都写入变更记录
- ✅ 设备删除联动：默认遇进行中占用返回 409 并点名是哪几段、不删任何数据；勾选强制删除后，先把未结束占用全部置为「已失效」并逐段写变更，再在同一事务内删除设备与其名下全部时段，不留孤儿时段。变更记录带设备编号快照，设备删除后仍能对号

### 2. 运营时段区间绑定
- ✅ 为设备分配固定开放使用时段
- ✅ 设置日期范围和时间范围
- ✅ 时段绑定状态管理
- ✅ 时段保存/解绑与设备停用/删除共用设备行悲观锁（多设备换绑按 id 升序加锁），并发下不会出现「时段还写成生效中、设备已停用或已删除」

### 3. 时段调整登记
- ✅ 修改设备对应时段
- ✅ 自动留存变更记录
- ✅ 记录变更前后值和操作人

### 4. 按时段筛选设备
- ✅ 根据时间范围筛选绑定的设备
- ✅ 支持查看设备详情和时段分配

### 5. 单设备全时段分配明细
- ✅ 查看单台设备绑定的全部时段区间
- ✅ 生成时段分配明细报告

### 6. 高峰同时在用上限
- ✅ 内置两段运营高峰窗：早出港高峰、晚进港高峰
- ✅ 按「航站楼分区 + 设备类型」配置高峰窗内同时在用上限
- ✅ 加绑/改时段时，与已有「生效中」时段在高峰窗内叠加且超上限即拦截，不落库
- ✅ 设备调区/改类型时：已开始的时段保持生效且不占新分区名额，尚未开始且超员的时段自动置为「已失效」，已结束的历史时段不动
- ✅ 上限配置行悲观锁，并发加绑只有一个能占到最后名额

## 启动命令

```bash
# 进入项目目录
cd /Users/Admin/Desktop/solo-0601/qyx-0701/qyx-组1/qyx-203

# Docker 启动（推荐）
docker compose up -d --build

# 或使用启动脚本
./start.sh
```

## 验证结果

### 后端单独编译
- 命令: `cd backend && mvn compile -q`
- 结果: ✅ 通过

### 前端单独构建
- 命令: `cd frontend && npm ci && npm run build`
- 结果: ✅ 通过

### Docker 构建启动
- 命令: `docker compose up -d --build`
- 结果: ✅ 通过

### localhost 与 127.0.0.1 验证
- localhost:8123: ✅ 返回正确页面
- 127.0.0.1:8123: ✅ 返回正确页面
- 一致性: ✅ 两者返回同一项目页面

### 浏览器页面和中文显示验证
- 前端页面: ✅ 正常显示
- 中文内容: ✅ 正常显示（设备类型、航站楼分区等）

### API功能验证
- 设备类型列表: ✅ 返回中文数据
- 航站楼分区列表: ✅ 返回中文数据
- 设备新增/查询/修改/删除: ✅ 全部通过
- 时段绑定: ✅ 新增和查询通过
- 统计分析: ✅ 按时段筛选和设备明细通过
- 变更记录: ✅ 自动记录变更

## 项目结构

```
qyx-203/
├── .env                    # 环境变量配置
├── .gitignore              # Git忽略配置
├── .dockerignore           # Docker忽略配置
├── docker-compose.yml      # Docker Compose配置
├── start.sh                # 启动脚本
├── qyx-203.md              # 项目文档
├── README.md               # 项目说明
├── backend/                # 后端项目
│   ├── pom.xml
│   ├── settings.xml        # Maven国内镜像配置
│   ├── Dockerfile
│   └── src/main/
│       ├── java/com/airport/maternity/
│       │   ├── controller/     # REST API控制器
│       │   ├── service/        # 业务逻辑层
│       │   ├── repository/     # 数据访问层
│       │   ├── entity/         # 实体类
│       │   ├── dto/            # 数据传输对象
│       │   └── config/         # 配置类
│       └── resources/
│           └── application.yml # 应用配置
└── frontend/               # 前端项目
    ├── package.json
    ├── package-lock.json
    ├── Dockerfile
    ├── nginx.conf
    └── src/
        ├── api/              # API接口
        ├── types/            # TypeScript类型定义
        ├── views/            # 页面组件
        ├── components/       # 通用组件
        ├── router/           # 路由配置
        ├── App.vue           # 根组件
        └── main.ts           # 入口文件
```

## Docker 分层缓存说明

- **后端**: 先 COPY settings.xml 和 pom.xml 下载依赖（华为云Maven镜像），再 COPY src 编译
- **前端**: 先 COPY package.json 和 package-lock.json 安装依赖（京东npm镜像），再 COPY 源码 build
- **依赖源**: 后端使用华为云 Maven 镜像，前端使用京东 npm 镜像
- **内存限制**: 后端 512MB，前端 256MB

## API接口列表

| 接口 | 方法 | 路径 | 说明 |
|------|------|------|------|
| 设备列表 | GET | /api/devices | 分页查询设备 |
| 设备详情 | GET | /api/devices/{id} | 查询单个设备 |
| 新增设备 | POST | /api/devices | 创建设备 |
| 修改设备 | PUT | /api/devices/{id} | 更新设备 |
| 删除设备 | DELETE | /api/devices/{id}?force=false | 删除设备（默认有进行中占用时 409 拦截；force=true 先失效未结束占用并写变更，再同事务清空时段与设备） |
| 时段列表 | GET | /api/time-slots | 分页查询时段 |
| 新增时段 | POST | /api/time-slots | 分配时段 |
| 修改时段 | PUT | /api/time-slots/{id} | 更新时段 |
| 删除时段 | DELETE | /api/time-slots/{id} | 删除时段 |
| 变更记录 | GET | /api/change-logs | 查询变更记录 |
| 按时段筛选 | GET | /api/statistics/by-time | 按时段筛选设备 |
| 设备明细 | GET | /api/statistics/device-detail/{id} | 设备时段明细 |
| 设备类型 | GET | /api/categories/device-types | 获取设备类型列表 |
| 航站楼分区 | GET | /api/categories/terminal-areas | 获取航站楼分区列表 |
| 高峰窗列表 | GET | /api/peak-capacity/windows | 获取运营高峰窗（早出港/晚进港） |
| 高峰上限列表 | GET | /api/peak-capacity | 上限配置及当前高峰同时在用数 |
| 新增高峰上限 | POST | /api/peak-capacity | 按分区+类型配置同时在用上限 |
| 修改高峰上限 | PUT | /api/peak-capacity/{id} | 调整上限 |
| 删除高峰上限 | DELETE | /api/peak-capacity/{id} | 删除上限配置 |